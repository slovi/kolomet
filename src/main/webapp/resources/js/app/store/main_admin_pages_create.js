require(['../common'], function (common) {
	
	require(['main_default_admin', 'input-file', 'fb'], function(mainDefaultPublic, inputFile, fb) {
		
		require(['form'], function(form) {
			
	    	inputFile.decorateId('page_files', {});		
	    	
    	});
	
	});

});