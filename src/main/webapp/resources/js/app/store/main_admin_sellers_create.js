require(['../common'], function (common) {
	
	require(['main_default_admin', 'input-file'], function(mainDefaultPublic, inputFile) {
		
		require(['form'], function(form) {
			
	    	inputFile.decorateId('sellerFiles', {});
			
    	});
	
	});

});